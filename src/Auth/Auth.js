import './Auth.css';
import Signup from '../Signup/Signup';

function Auth() {
  return (
    <div className="Auth">
      <header className="Auth-header">
        <p>
          Edit <code>src/Auth.js</code> and save to reload.
        </p>
      </header>
      <body>
        <Signup />
      </body>
    </div>
  );
}

export default Auth;
